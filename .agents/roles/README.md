# Codex Employee Agents

These files define the Codex-only employee roles for this Salesforce Agentforce project. Codex remains the execution manager; each role is a reusable instruction contract for a phase of the work.

Use the roles through `AGENTS.md` and keep the workflow evidence in `.task/current/`.
