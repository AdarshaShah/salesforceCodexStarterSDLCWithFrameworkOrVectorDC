# Salesforce Org Notes

Store safe, non-secret org notes here, such as:

- org alias names
- enabled feature observations
- package or permission prerequisites
- validation command outcomes

Do not store credentials, auth URLs, session IDs, access tokens, refresh tokens, or private keys.
