# Retrieved Context

This optional scratch file can collect the most relevant context for the current task.

Because this project uses file-backed context only, Codex should build this from repo files and `.context/` notes when useful. It is not generated by a vector database.
