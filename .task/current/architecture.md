# Architecture

## Impacted Areas

- `AGENTS.md`
- `.context/governance/`
- `.context/knowledge-base/`
- `.task/current/`
- `README.md`

## Design

Add a compliance checklist and process-exception policy as explicit closeout gates. Add a future-scope register for limitations that remain outside the Codex-only, file-backed setup. Update README so the operating model is discoverable without reading every process file.

## Rollback

Revert the process hardening commit if the added governance layer is too heavy for the project.
